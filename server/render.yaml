services:
  - type: web
    name: modern-art-server
    runtime: python
    plan: free
    buildCommand: pip install -r server/requirements.txt
    startCommand: cd server && python main.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
